services:
  - type: web
    name: najemnik-demo
    runtime: docker
    plan: free
    healthCheckPath: /healthz
    autoDeploy: false
    envVars:
      - key: DEMO_MODE
        value: "true"
      - key: APP_VERSION
        value: portfolio-demo-1
      - key: COOKIE_SECURE
        value: "true"
